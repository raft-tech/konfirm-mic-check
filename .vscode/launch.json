{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Go Test",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${fileDirname}",
            "env": {
                "PATH": "${env:PATH}:${workspaceFolder}/bin"
            },
            "presentation": {
                "hidden": false,
                "group": "test",
                "order": 1,

            },
            "preLaunchTask": "go: build inspections",
        }
    ]
}